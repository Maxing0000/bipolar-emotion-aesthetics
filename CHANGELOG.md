# 更新日志

## v2.8.0（2026-09-17）— 易用性优先的精简重构版：AI 技能体验全面优化

> **版本说明**：v2.8.0 是基于 v2.14.0 的**精简重构版**，核心目标是提升 AI 技能的易用性。版本号回退是为了标记这是一个新的"易用性优先"版本线，功能上是 v2.14.0 的超集（保留所有核心能力，同时大幅优化体验）。

### 核心优化

- **SKILL.md 大幅精简**：从 419 行精简到 235 行（减少 44%），去除冗余信息，突出核心流程
- **图片分析流程简化**：从 9 步简化为 5 步，降低 AI 执行复杂度，提升响应速度
- **标准化输出格式**：统一"结论先行"结构（一句话结论 → 关键数据 → 维度分析 → 病症诊断 → 改进处方 → 你可能还想问）
- **增加降级处理**：图片不清/信息不足时，AI 主动说明并给出替代方案，而不是强行分析
- **增加追问引导**：每次输出结尾给出 2-3 个用户可能想问的问题，降低用户思考成本
- **集中展示新手示例**：6 个标准示例提问集中展示，用户一看就知道怎么用
- **量化引擎命令移到"AI 内部使用"**：普通用户不需要知道命令，AI 自动调用

### 新增

- **各平台统一文案包** `PLATFORMS-UNIFIED-COPY.md`：统一所有平台的名称、描述、示例、Logo，包含 8 个平台的具体文案和更新检查清单
- **通用系统提示词更新** `platforms/universal-system-prompt.md`：增加轻量级快速诊断模式、模式选择规则、输出结尾追问引导
- **轻量级快速诊断模式**：触发词"快速看看/简单诊断/大概怎么样"，只输出 W(T)+范式+主辅比+一句话结论，速度快一倍

### 统一

- **版本号统一**：SKILL.md、manifest.json、SKILLHUB.md 全部统一为 v2.8.0
- **品牌名称统一**：所有平台显示名称统一为"双极情绪美学 BEA"
- **描述文案统一**：标准版（120字）、简短版（50字）、一句话版（30字）三档统一
- **示例提问统一**：6 个标准示例提问在所有平台通用
- **核心能力标签统一**：图片分析、病症诊断、设计优化、美感生成、方案对比、快速诊断

### 测试

- 全量自测通过
- 图片分析 5 步流程验证通过
- 轻量级模式输出格式验证通过
- 各平台文案兼容性验证通过

## v2.14.0（2026-09-16）— 撤销图片分析引擎化与判断协议，回归 AI 直接判断

### 撤回

- **图片分析引擎化（v2.11.0）与标定器（v2.12.0）整体撤除**：删除 `scripts/bea_image.py`（像素→t 值程序化通道）、`scripts/bea_rubric.py`（视觉评分标尺）、`scripts/bea_calibrate.py`（人工标定回归 CLI），以及 MCP 工具 `bea_analyze_image` / `bea_rubric`。
  - 理由：像素特征线性映射算出的 t 值只是"能算"，不等于"看得准"。审美判断以 AI 直接观图的判断力为准，不用程序化通道替代它。
- **判断协议（v2.13.0）撤除**：删除 `references/07-judgment-protocol.md`；SKILL.md 执行流程移除"步骤 0：判断协议"，输出格式中的"证据层级/自我反驳"等条目一并回退，正文恢复为 v2.10.0 状态。
- **MCP 工具补全（v2.9.0）撤回**：MCP Server 回到 3 个工具（`bea_analyze` / `bea_compare` / `bea_dimensions`）。引擎内部的调整建议、美感生成、灵敏度、批量算法保持可用，CLI 与技能侧不受影响。
- 打包：CORE_FILES 与 npm / PyPI 包移除上述脚本与文档；`pyproject` 移除 `image` 可选依赖（Pillow）。

### 保留

- 分发渠道不变：npm（`npx -y bea-mcp`）与 PyPI（`pipx install bea-mcp`）继续可用，本次同步发布 2.14.0。
- 技能侧读图能力不变：`references/06-image-anchors.md` 与 SKILL.md 图片分析标准流程保持原样，由 AI 观图逐维度打分后交引擎计算 W(T)。

### 测试

- 全量自测 143 → 113 项（移除标尺 / 图像 / 标定测试段）；MCP 会话自测 41 → 22 项（三工具全链路）。版本 2.13.0 → 2.14.0。

## v2.13.0（2026-09-16）— 判断协议：公共判准与大师推理顺序

### 新增
- **《BEA 判断协议（公共判准）》`references/07-judgment-protocol.md`**——不是任何人的品味偏好，而是可公开检验的判断程序：
  - **三条公共性原则**：可复现（同证据同结论）、有证据（落到可指认的形式事实）、可反驳（写明何种证据会改变判断）
  - **判断四步（顺序不可颠倒）**：定调 → 定决定性维度（看主导维度不看平均分）→ 找最泄气处（说不出最致命一处＝没看清）→ 出处方（改哪里/什么量级/预期/代价）
  - **证据层级裁决**：L1 可测量形式事实 > L2 感知共识 > L3 语境解释（须声明适用人群）> L4 个人偏好（不参与定案）；同级取"更可指认"；无法归入 L1/L2 的判为**待验证**
  - **三条反和事佬纪律**：先结论后依据、必须指出最致命一处、自我反驳一轮；禁止用"审美是主观的"结束讨论
  - **一页纸判断输出模板**（定调/决定性维度/最泄气处/证据层级/处方/自我反驳/待验证）
- SKILL.md 执行流程新增"步骤 0：判断协议"摘要，完整模式输出格式对齐协议模板；注意事项由"打分有主观性…供参考"改为"证据与推理必须公开可复核"
- 打包清单纳入 07-judgment-protocol.md；版本 2.12.0 → 2.13.0

## v2.12.0（2026-09-16）— 标定器：让像素通道从「能用」变「可信」

### 新增
- **标定器 `scripts/bea_calibrate.py`**（维护者 CLI，纯标准库）：
  - `collect`：对图片目录逐张提取特征与程序 t 值，产出待标注 CSV（t_human 列留空）；品类可按 `<品类>_<描述>.png` 文件名自动推断
  - `fit`：逐维度最小二乘拟合 t_human ≈ a·t_program + b，产出 `data/calibration.json`；样本不足（<3）或无方差的维度自动跳过并说明原因
  - `status`：查看当前标定状态与拟合质量（R²、样本数、优/良/差评级）
- **bea_image 标定层**：自动加载 `data/calibration.json`（路径可由 `BEA_CALIBRATION` 环境变量指定），逐维度仿射修正 t_final = clip(a·t_program + b)；未标定时行为与 v2.11.0 出厂线性表完全一致
- 自测 134 → 143 项（有 Pillow）/ 137（无 Pillow）：OLS 系数恢复、fit 全链路（含非法维度拒绝与未标注行跳过）、标定前后 t 值平移验证
- 打包清单纳入 bea_calibrate.py；版本 2.11.0 → 2.12.0

## v2.11.0（2026-09-16）— 图片分析引擎化：从像素到 W(T) 的程序化通道

### 新增
- **`bea_rubric`（MCP 工具）**：视觉评分标尺模块 `scripts/bea_rubric.py`——5 品类 × 引擎 canonical 维度名的观察点与 t=2/5/8 三档锚点，AI 看图打分的标准依据（移植自旧 PyPI 包 rubric_data.py，按 v2.x 维度名重写并补 building 品类）
- **`bea_analyze_image`（MCP 工具）**：图像特征引擎 `scripts/bea_image.py`——程序解码图片，提取七项视觉特征（锐度/对称/纹理/光影硬度/色彩强度/色温/构图重心），线性映射为维度 t 值后走引擎全链路，返回精确 W(T)
  - Pillow 为可选依赖：PyPI `pipx install "bea-mcp[image]"`；未安装时该工具优雅报错，其余工具不受影响
  - ui 品类「动效」维度静态图无法测量，取中性值 5 并在结果中标注
  - 特征→t 值为 v1 人工标定，随案例库积累可继续回归校准

### 测试与工程
- 自测扩至 **134 项**（引擎，含标尺一致性回归：RUBRICS 维度名与 CATEGORY_WEIGHTS 严格比对；图像端到端在无 Pillow 环境自动跳过）
- MCP 会话自测 **41 项**（rubric 维度一致性、图片端到端/无 Pillow/文件不存在三条路径）
- 打包：bea_rubric.py / bea_image.py 进 CORE_FILES 与 npm/PyPI 包；pyproject 新增 `[project.optional-dependencies] image`

## v2.10.0（2026-09-16）— npm / PyPI 分发 + 官网 MCP 安装引导

### 新增
- **npm / PyPI 分发包**（`package.py --npm --pypi`，产物在 dist/，零重复文件）：
  - npm `bea-mcp`：bin.js 拉起 python3 运行 server.py，客户端配置一行接入（`npx -y bea-mcp`）
  - PyPI `bea-mcp`：console script `bea-mcp`（`pipx install bea-mcp`）
  - server.py 引擎定位升级为三级回退：环境变量 BEA_ENGINE → 仓库结构 ../scripts/ → 同目录（pip 安装布局）
- **官网 MCP 安装引导**（docs/index.html）：平台表新增 MCP Server 卡片；SkillHub 安装块下新增 MCP 接入块，npx / pipx 两种配置一键复制（含降级复制兼容）
- 本地验证：npm tarball 安装 → bin 握手 + 7 工具；pip 安装 → console script 握手 + bea_compare 调用，均通过

### 变更
- **PyPI `bea-mcp` 收编升级**：该名下原为 2026-09-13 发布的旧 1.x（官方 SDK 版，维度命名与现引擎不同源）；本版作为全新引擎版取代之，版本号沿用仓库版本（2.10.0 > 1.2.0，可平滑升级）
- 版本全线 2.9.0 → 2.10.0

## v2.9.0（2026-09-16）— MCP 工具补全：七个工具覆盖引擎全能力

### 新增
- **MCP 新增四个工具**（3 → 7），引擎的进阶能力全部可被 AI 客户端调用：
  - `bea_suggest` — 调整建议：给定当前打分与目标（范式名或 W(T)），返回逐步调分方案 + 调整后验证
  - `bea_generate` — 美感生成：给定目标自动生成最优维度配置（balanced / focused / distributed 三策略）
  - `bea_sensitivity` — 灵敏度分析：每维度 t±step 对 W(T) 与四维评分的差分影响，找出最值得改的维度
  - `bea_batch` — 批量分析：多对象一次分析并按 W(T) 排名
- MCP 自测扩至 34 项（新增四工具行为校验 + 非法策略 / 空 items 错误路径）

### 变更
- MCP 自测数在文档中的表述同步；版本全线 2.8.0 → 2.9.0

## v2.8.0（2026-09-16）— MCP Server：从提示词技能到软件产品

### 新增
- **MCP Server** `mcp-server/`：纯 Python 标准库实现 MCP stdio 服务器（JSON-RPC 2.0，零第三方依赖，离线可用），暴露三个工具——`bea_analyze`（单对象分析：W(T)/范式/病症/调分建议）、`bea_compare`（A/B 对比：逐维度差值 + 引擎报告）、`bea_dimensions`（品类维度/权重/范式锚点查询）；任意 MCP 客户端（WorkBuddy / Claude Desktop / Cursor 等）即可精确调用引擎
- **MCP 自测** `mcp-server/test_server.py`：起真实子进程走完整 MCP 会话（握手 → 工具列表 → 三工具调用 → 错误路径），20 项检查，工具返回结果与引擎直算逐一比对

### 修复
- **引擎 bug（MCP 自测发现）**：`format_report_markdown` 在有病症时引用不存在的 `d['identify']`/`d['mechanism']` 键导致 KeyError，已对齐病症字典真实键（`evidence`/`prescription`）；自测套件补 4 项回归（有病症/无病症 markdown 报告、对比报告、病症触发确认），自测总数 109 → 113

### 变更
- CI 增加 MCP 相关关卡：mcp-server 编译检查、pyflakes、MCP 会话自测
- 版本号全线对齐 2.8.0（引擎 / manifest / SKILL.md / 官网 / FAQ / 各文档页脚）

## v2.7.0（2026-09-15）— WorkBuddy 平台支持与文档全面引导

### 新增
- **WorkBuddy 平台适配**：新增 `platforms/workbuddy/`，多平台支持表加入 WorkBuddy（原生技能 + 多模态图片分析）
- **图片分析方法论升级**：SKILL.md 补齐 9 步标准图片分析流程、打分一致性要求（同图 t 值差异 ≤1、W(T) 差异 ≤0.1）、图片分析输出模板、各品类分析重点与 5 条常见误区
- **案例库 Web 应用** `docs/case-library.html`：30 个案例，支持搜索 / 筛选 / 排序 / 详情查看
- **图片分析助手 H5** `docs/image-analyzer.html`：上传图片 → 六项图像特征提取（边缘锐度 / 对称性 / 纹理复杂度 / 光影硬度 / 色彩温度 / 构图重心）→ BEA 映射打分 → W(T) → 完整报告

### 文档
- 全面优化文档，最大限度引导用户用自然语言使用 BEA（SKILL.md / README / QUICKSTART 重写使用引导）
- 官网与案例库增加各 AI 平台的使用引导，移除官网内的图片分析入口、统一引导至 BEA 技能

### 资源
- 30 个案例实现图片 100% 覆盖（含 AI 补图），统一 16:9 比例、压缩体积并支持 WebP
- 统一 favicon 与 logo，移动端自适应布局与触摸交互优化

### 修复
- 统一版本号：SKILL.md 尾部、`docs/index.html` 页脚、`templates/output-templates.md`、`bea_quant.py` 头注释全部对齐至 2.7.0
- `scripts/package.py`：版本号改为从 `manifest.json` 单一来源读取；修正打包清单（`LICENSE` → `LICENSE.md`、补入 `references/06-image-anchors.md`、移除已下线的安装脚本）；新增打包前预检（`--check`），缺失文件直接报错退出，避免产出缺文件的包
- **上传包移除二进制文件**：SkillHub 禁止上传二进制文件（png/jpg 等会被平台跳过并警告），打包清单不再包含 `assets/` 下的 logo 图标——图标在 SkillHub 网页端单独上传；`package.py` 预检新增二进制文件拦截，命中直接报错退出
- 修正文档中指向已移除安装脚本的失效命令（README / QUICKSTART / FAQ），改为技能市场安装 + 手动克隆两条可用路径
- 补齐 CHANGELOG 中缺失的 v2.6.0–v2.6.4 版本记录

### README 重构与文档一致性（第二轮）
- **README 重写**：628 行 → 约 330 行，新增目录导航与读者分层（普通用户「30 秒上手」在前，开发者「命令速查」在后）；8 个命令的详细输出示例压缩为速查表，维度权重 5 张表合并为 1 张，FAQ 精选后外链 `FAQ.md`
- **修正失实描述**：平台表补入 WorkBuddy；徽章平台数 6+ → 7+、案例数统一为 30+；「六维分析」的旧说法改为随品类变化的「维度极性审计」；README 示例数据改用脚本实测输出
- **`manifest.json` 修正品类声明**：`supported_categories` 由 10 个改为引擎实际支持的 5 个（phone / car / brand / ui / building），新增 `documented_domains` 记录案例库覆盖的九大领域；描述补充 WorkBuddy / SkillHub
- **版本号彻底收口**：`bea_quant.py` 新增 `__version__` 常量，输出脚注、CLI 标题、`--help` 描述全部改为动态引用（原先硬编码 v2.4.0 / v2.2.1）；自测新增「头注释版本号与 `__version__` 一致」断言（自测 108 → 109 项）
- **测试数表述去数字**：文档中「108 项自测」统一改为「全量自测」，避免每次增删测试都要改文档（`FAQ.md` 页脚版本号同步至 2.7.0）

### 内容一致性（第四轮）
- **案例库单一数据源**：案例数据从网页 JS 字面量抽出为 `data/cases.json`（唯一数据源），新增 `scripts/gen_case_library.py` 从 JSON 重新生成 `docs/case-library.html` 数据段（`--check` 校验一致性）；CI 增加对应检查步骤，网页数据被手改会直接报错
- **补齐网页缺失的 3 个对比案例**：网页原 29 案例（缺 iPhone vs 游戏手机、奔驰S vs 宝马7、中银 vs 汇丰），md 原缺 Tesla Cybercab——合并后双侧均为 **32 个案例**（ids 1–32 连续），md 补写案例32 Cybercab，速查表/类型分类/时代分类/范式覆盖统计同步更新
- **修正写死的统计数字**：网页页头「案例总数 30」改为随数据生成（32）；SKILL.md、README 的案例数表述（31→32）对齐
- CHANGELOG 历史条目中的旧数字（v2.6 时期的 30 个案例）按惯例保留不改

### 架构优化（第四轮）
- **自测试拆出引擎**：109 项自测从 `bea_quant.py`（2798 行）整体迁移至 `tests/test_be.py`，引擎瘦身至 2437 行；`test` 子命令改为委托执行，入口与行为不变（`python3 scripts/bea_quant.py test`）；`tests/test_be.py` 加入打包清单，随技能包分发
- **维度表单一数据源**：`bea_guide.py` 删除手工维护的维度副本（上轮 phone 3 个维度不一致 bug 的根因），改为 importlib 动态加载引擎并读取 `CATEGORY_WEIGHTS`——引擎增删品类/维度，引导脚本自动跟随
- **新增 GitHub Actions**：`.github/workflows/ci.yml`——双 Python 版本（3.9/3.13）矩阵，编译检查、pyflakes（仅 scripts/，tests 符号运行时注入不做静态检查）、全量自测、打包预检四道关卡
- 校验：三条 guide 路径（report/compare/interactive）冒烟通过，8 个上传包重打全绿（29 文件，无二进制）

### 仓库卫生（第四轮）
- **构建产物出库**：`dist/` 从 git 跟踪中移除（原 30 个文件，含 21 个 v2.5.0–v2.6.1 旧包）并加入 `.gitignore`；本地旧包已删，仅保留 v2.7.0 的 8 个包
- **截图出库**：`.gitignore` 的 `_shots/` 规则改为 `**/_shots/`，`case-library/_shots`、`image-analyzer/_shots` 共 6 张截图移出跟踪
- **删除 11 个空目录骨架**：`tests/`、`ci/`、`mcp-server/bea_mcp/`、`bea-website/`（含空 assets/_shots）、嵌套的 `bipolar-emotion-aesthetics/`、`docs/paper/`、`.github/workflows/`——全部为无文件的误导性空壳
- 校验：文档无对已删目录的引用，打包预检与全量自测（109 项）通过

### 代码与文档全面体检（第三轮）
- **修复 `interactive` 子命令崩溃**：`bea_quant.py interactive` 调用了不存在的 `run_interactive()`（pyflakes 报 undefined name），现委托给同目录 `bea_guide.py`，导入 `subprocess` 实现
- **修复 `bea_guide.py` 维度表与引擎不一致**：phone 品类 6 个维度中 3 个、brand 2 个、ui 1 个用旧维度名（如「形状线条」），走交互式引导会让引擎报错；已全部对齐 `CATEGORY_WEIGHTS`
- **修复 WorkBuddy 适配文档事实错误**：`platforms/workbuddy/README.md` 把 WorkBuddy 误写成「字节跳动推出的 AI 工作助手，深度集成飞书办公生态」，使用场景、对比表、FAQ 全部基于该错误前提重写；SKILL.md 平台表「飞书集成」改为「MCP 连接器」
- **文档维度名全面对齐引擎**：SKILL.md 打分表、`references/05-case-studies.md` 案例打分表（14 处）、`references/02-workflow.md`、`templates/output-templates.md` 共 24 处旧维度名（形状线条/构图比例/图形形状/版式构图/布局留白等）替换为引擎 canonical 维度名，照案例抄 t 值不再报错
- **修复 SKILL.md 死引用**：`platforms/coze/system-prompt.md` 不存在，改指 `platforms/universal-system-prompt.md` + `platforms/coze/README.md`
- **代码静态检查清零**：pyflakes 全清——24 处无占位符 f-string、2 处未使用变量（`secondary` 改为实际参与主辅比计算、`p_lp` 冗余赋值移除）
- **16 个 CLI 子命令逐一冒烟验证**：analyze/report/score/suggest/sensitivity/compare/batch/multigroup/style-cycle/template/generate/profiles 三件套/test/interactive 全部可运行
- **全仓库交叉引用核查**：33 处文档内文件引用全部存在；案例数（31）、平台表（7+SkillHub 渠道）、版本号（2.7.0）全线一致
- **新增 SkillHub 上传包**：`scripts/package.py --skillhub` 生成 SkillHub 上传用 zip，`--all` 会一并生成

### SkillHub 合规（第三轮）
- **补齐 SKILL.md frontmatter 必填字段**：新增 `slug: bipolar-emotion-aesthetics`、`displayName: 双极情绪美学 BEA`（官方 release.md 将这两项列为发布阻断项），并补 `summary` / `tags` / `homepage` 建议字段
- **修正上传包结构**：SkillHub 要求"ZIP 根目录包含 SKILL.md"，`--skillhub` 包由"外层套技能目录"改为平铺结构（此前按"技能目录"直觉实现，可能不被识别）
- **`package.py` 内置 SkillHub 规范校验**：新增 `check_skillhub_spec()`，校验 frontmatter 必填字段、slug 是否 kebab-case 且长度 2–128、version 是否合法 SemVer 且与 `manifest.json` 一致；`--check` / `--skillhub` / `--all` 均会执行，不合规直接报错退出
- **安全自查**：扫描上传包内全部文本文件，确认无密钥、本地绝对路径与内网地址（对应 TRACE 评测的 Trust 红线维度）
- `SKILLHUB.md` 重写发布章节：补充官方三种发布途径（网页 / CLI / Agent 对话，WorkBuddy 在支持列表内）、平台规范要求表、TRACE 评测说明、CLI 发布不带图标的注意事项

## v2.6.4（2026-09-15）— 案例库丰富（第三阶段）
- 新增 5 个案例：热门产品 + 经典案例——华为 Mate 60 Pro、蔚来 ET9、大众甲壳虫、包豪斯设计、迪士尼乐园

## v2.6.3（2026-09-15）— 案例库丰富（第二阶段）
- 新增 5 个案例：失败案例 2 个（某电竞游戏手机、某新势力轿车）、对比案例 2 个（iPhone Duo vs 游戏手机、奔驰 S 级 vs 宝马 7 系）、跨模态案例 1 个（苹果全家桶产品+界面+声音）

## v2.6.2（2026-09-15）— 案例库丰富（第一阶段）
- 新增 5 个案例，覆盖六大范式

## v2.6.1（2026-09-15）

### 新增
- `references/06-image-anchors.md`：图片分析锚点示例与评分一致性指南（标准流程、视觉锚点速查表、完整示例、校准方法）
- 官网新增图片分析章节与轻量级快速诊断模式说明

### 修复
- 修正 SKILL.md description 中的版本号

## v2.6.0（2026-09-15）— 轻量级诊断模式

### 新增
- **轻量级快速诊断模式**：触发词「快速看看 / 简单诊断 / 大概怎么样 / 快速分析」，只输出 W(T) + 范式定位 + 主辅比 + 一句话结论，速度快一倍，减少长上下文导致的指令遗忘
- `manifest.json` 新增 `quick_diagnose` 能力声明

### 变更
- SkillHub 同步适配：清理不支持的文件格式（.png 截图、.DS_Store 等），确保上传不丢文件
- 修正 SKILL.md frontmatter 位置（移动到文件最开头）
- 仓库不再内置 `install.sh` / `install.ps1` 等本地安装脚本，安装统一走技能市场

## v2.5.0（2026-09-15）— 核心算法范式升级

### P0 严谨性升级
- **主辅比软划分**：从硬边界（t<=3亲极/4-5中性/t>=6危极）改为模糊隶属度软划分，避免 t=3 和 t=4 被划分为不同极性的突变问题
- **病症诊断完整优先级体系**：建立四级优先级（一票否决/严重/中等/轻微），新增互斥关系（刺激疲劳覆盖攻击症、甜腻症覆盖张力不足症），诊断结果按优先级排序
- **语境适配评分优化**：加入 W(T) 与品类最优区间匹配度评分，在品类最优区间内加3分，偏离则扣分（偏高扣分更重）

### P1 有效性升级
- **调整建议最优性验证**：suggest_adjustment 返回调整后验证信息（实际W(T)、偏差、调整维度数、总调整级数、是否达到目标），JSON 和文本输出均包含验证信息
- **跨模态一致性精细化评分**：从离散四档（90/70/50/30）改为连续评分（基础分随W(T)差异连续变化 + 主导极性一致加分），更精细可比较

### 新增核心功能
- **美感生成（generate命令）**：给定目标 W(T)/范式，自动生成最优维度配置。支持三种策略：
  - balanced（均衡）：各维度差异适中，高权重维度略高
  - focused（集中）：1-2个高权重维度承担主要危极
  - distributed（分散）：各维度均匀提升，制造适度差异
- 自动微调使 W(T) 接近目标（偏差≤0.01），避免极端值（t=0/10），输出可直接复制的命令

### 测试
- 从 102 项增加到 108 项，全部通过
- 新增测试：主辅比软划分、病症优先级互斥、语境适配匹配度、调整建议验证、美感生成（三种策略）

## v2.4.1（2026-09-15）

### 算法严谨性提升（P0）
- **主辅比得分边缘修正**：primary_pct 在 86-90 时得分从12分提升到14分，与 is_balanced=True 判断一致
- **新增"本能越界"病症**：任何维度 t>=10 自动诊断，阈值安全一票否决（0分），耐看性病症影响扣25分
- **新增"刺激疲劳"病症**：所有维度 t>=7 且 W(T)>=0.65 自动诊断，耐看性扣12分，双极张力扣3分

### 算法实用性提升（P1）
- **灵敏度综合公式优化**：从纯 W(T) 灵敏度改为 0.7×W(T)灵敏度 + 0.3×评分灵敏度，更全面反映调整对最终美感的影响；输出新增 wt_sensitivity 和 score_sensitivity 分项
- **调整建议新增策略选项**：--strategy focused（集中，默认，改动维度少）/ distributed（分散，每个维度调整量小）
- **耐看性维度稳定性使用加权标准差**：高权重维度的波动对耐看性影响更大，更符合实际

### 测试
- 从 92 项增加到 100 项，全部通过
- 新增测试：本能越界诊断、刺激疲劳诊断、调整策略对比、灵敏度分项

## v2.4.0（2026-09-15）

### 全方位审查修复（v2.4.0 发布后）
- **主辅比 is_balanced 判断修正**：从 `primary_pct >= 60` 改为 `60 <= primary_pct <= 90`，全亲极/全危极（100:0）不再误判为 balanced；新增 `has_subordinate` 字段，区分"从属极不足"和"主辅不足"两种警告
- **t=10 越阈值安全警告**：report 输出新增【安全警告】区块，检测 t≥10 的维度并给出⛔警告，建议降到 t≤9
- **suggest 极端调整警告**：当 |ΔW(T)|>0.5 时给出⚠警告，建议分阶段调整
- **风格周期律百分位表述修正**：百分位≥100 改为"超过案例库所有样本"，≤0 改为"低于案例库所有样本"
- **自定义权重维度不匹配错误信息改进**：添加"如使用了自定义权重，请检查权重配置中的维度名是否一致"的提示
- **版本号统一**：bea_quant.py 头部和 CLI description 从 v2.3.0 统一为 v2.4.0
- **测试扩充**：从 85 项增加到 92 项，新增主辅比边界、t=10 越阈值等测试

### 新增功能
- **主辅比计算**：BEA 理论核心"主辅基底"的量化实现
  - 自动计算亲极/危极/中性的权重占比
  - 输出主导极性（亲极 P+ / 危极 T− / 中性均衡）
  - 输出主辅比（如 "72:28"）
  - 自动判断是否达到主辅比 ≥6:4（BEA 理论要求）
  - report 输出新增【主辅比】区块
  - JSON 输出新增 polarity_ratio 字段

- **耐看性评估**：衡量设计长期使用/观看是否容易疲劳
  - 四维度评估：W(T)水平（30分）、维度稳定性（25分）、病症影响（25分）、主辅比（20分）
  - 不同品类有最优 W(T) 区间（手机 0.18-0.35，汽车 0.30-0.48）
  - 评级：极耐看（≥80）、耐看（65-79）、一般（50-64）、易疲劳（<50）
  - 自动给出改进建议
  - report 输出新增【耐看性】区块

- **跨模态一致性检查 + 层级嵌套分析（multigroup 命令）**
  - 输入多组维度数据（外形/内饰/声音，或宏观/中观/微观）
  - 跨模态一致性：检查各组 W(T) 和主导极性是否一致
  - 层级嵌套分析：判断是同向叠加（强化型）、微差补偿（高级型）还是反向冲突（需修正）
  - 支持 --json 输出

- **风格周期律判断（style-cycle 命令）**
  - 基于案例库 W(T) 分布，判断当前设计在风格周期中的位置
  - 四种位置：滞后（偏保守）、主流（安全区）、领先（适度超前）、激进（高度超前）
  - 给出当前趋势方向和风格周期建议
  - 支持 --json 输出

- **自定义权重配置管理**
  - save-profile：保存自定义权重配置到 ~/.bea_profiles.json
  - list-profiles：列出所有保存的配置
  - delete-profile：删除保存的配置
  - 所有分析命令支持 --profile 参数加载配置
  - 优先级：--profile > --weights > 品类默认

### 算法改进
- **suggest_adjustment 算法优化**：
  - 旧算法：连续调整同一维度到极限（如曲面 9→1），导致极端结果
  - 新算法：每轮只调整1级，然后重新计算优先级，让多个维度均衡调整
  - 避免把维度调到 0 或 10（极端值）
  - 效果：Cybertruck 调整到均衡典雅时，从"曲面9→1、特征线9→1"变为"5个维度均衡调整，无极端值"

- **语境适配评分优化**：
  - 从固定18分改为按品类给基准分（手机17、汽车18、品牌16、UI17、建筑19）
  - 按范式调整（亲和精致+2、均衡典雅+2、冷峻克制-1、先锋反叛-2）
  - 明确标注为"待人工评估"，给出5项人工检查清单

- **灵敏度分析支持多步长**：
  - 新增 --step 参数（1/2/3），默认1
  - step=2/3 适合大幅调整或探索性分析
  - 输出包含 step 字段，表头动态显示 t+step/t-step

### 统一与修复
- 版本号统一：bea_quant.py 头部和 CLI 从 v2.2 统一为 v2.3.0
- 署名统一为马星
- 官网计算器维度名统一为标准名（曲面/特征线/灯组/比例/材质）
- JSON 输出统一：report/score/suggest 命令新增 --json 参数
- install.sh 版本号更新为 v2.3.0
- FAQ.md 全面更新，新增 Q20-Q26 共7个新功能相关问题

### 测试
- 自测试从 56 项增加到 85 项，全部通过
- 新增测试：耐看性（5项）、语境适配（3项）、灵敏度step=2（4项）、多组分析（4项）、风格周期律（4项）、自定义权重（4项）、JSON输出（3项）、building品类（2项）

## v2.3.0（2026-09-14）

### 新增功能
- **灵敏度分析（sensitivity 命令）**：v3.0 务实版核心功能
  - 计算每个维度 t±1 对 W(T) 和四维评分的影响
  - 找出"改动哪个维度效果最明显"，避免盲目试错
  - 用有限差分法实现（不需要梯度下降），技术上完全可实现
  - 支持 `--target` 参数：计算朝目标范式方向最有效的调整
  - 支持 `--json` 输出，方便程序集成
  - 输出包含：核心结论（最值得改的3个维度）、朝目标方向最有效调整、详细灵敏度表、使用建议

- **主辅比计算**：BEA 理论核心"主辅基底"的量化实现
  - 自动计算亲极/危极/中性的权重占比
  - 输出主导极性（亲极 P+ / 危极 T− / 中性均衡）
  - 输出主辅比（如 "72:28"）
  - 自动判断是否达到主辅比 ≥6:4（BEA 理论要求）
  - 主辅不足时提示"可能存在均分症，情绪暧昧"
  - report 输出新增【主辅比】区块
  - JSON 输出新增 polarity_ratio 字段

### 算法改进
- 灵敏度分析的核心洞察：W(T) 变化量 = 权重/10，与当前 t 值无关（只要不在边界）
- 评分变化量通过实际重新计算四维评分获得，能发现"提升某维度会引入病症"的情况
- 边界值处理：t=9 时 t+1 变化为0，t=1 时 t-1 变化为0

- **suggest_adjustment 算法优化（v2.3.0 第二版）**：
  - 旧算法：连续调整同一维度到极限（如曲面 9→1），导致极端结果
  - 新算法：每轮只调整1级，然后重新计算优先级，让多个维度均衡调整
  - 避免把维度调到 0 或 10（极端值），除非目标差距过大
  - 合并同一维度的连续调整为一步输出
  - 效果：Cybertruck 调整到均衡典雅时，从"曲面9→1、特征线9→1"变为"5个维度均衡调整，无极端值"

### 统一与修复
- 版本号统一：bea_quant.py 头部和 CLI 从 v2.2 统一为 v2.3.0
- 署名统一：代码头部 Copyright 统一为"马星"
- 维度名统一：官网计算器从旧维度名（形体曲面动势/特征线条等）统一为标准维度名（曲面/特征线/灯组/比例/材质）
- JSON 输出统一：report/score/suggest 命令新增 `--json` 参数，所有命令支持 JSON 输出

### 测试
- 自测试从 50 项增加到 56 项，全部通过
- 新增测试：灵敏度分析返回所有维度、按灵敏度降序排列、包含必要字段、高权重维度灵敏度最高、带目标包含 toward_target、边界值处理
- 新增测试：主辅比包含必要字段、亲极/危极主导判断、主辅比格式、全中性时 is_balanced 为 False

### 文档
- SKILL.md 版本号 v2.2 → v2.3，描述添加灵敏度分析和设计优化
- 命令速查添加 sensitivity 命令示例
- FAQ.md 添加灵敏度分析相关问题

## v2.2.2（2026-09-14）

### 新增功能
- **四维评分精细化**：从离散分值改为精细化评分
  - 双极张力：基础分15 + W(T)区间 + 维度极差 + 明确对比 - 病症扣分
  - 结构秩序：基础分15 + 焦点数量(1-2个最佳) + 无均分症 + 无重点通胀 - 甜腻症扣分
  - 阈值安全：基础分20 + W(T)安全区间 - 越界扣分 - 极端值扣分
  - 语境适配：参考分18 + 5项人工检查清单
  - 每个维度都有评分明细（基础分、各加扣分项及具体值）
  - 新增评级（成熟作品/良好/中等/存在明显问题）和短板提示

- **compare/batch JSON输出**：添加 `--json` 参数
  - compare JSON：两个产品完整分析 + 对比信息（wt_diff、paradigm_diff、dimension_diffs）
  - batch JSON：所有产品分析 + 统计信息（wt_min/wt_max/wt_avg）
  - 方便程序集成和自动化分析

- **自测试扩展**：从27项增加到42项
  - suggest 调整策略测试（降低危极优先降高t值、提升危极优先升低t值）
  - 攻击症新条件测试（W(T)=0.6全6分应诊断攻击症）
  - 边界值测试（W(T)=0.15/0.30/0.48/0.60/0.66 范式定位）
  - 四维评分测试（评分范围0-25、总分范围0-100、包含评分明细）
  - compare/batch 解析测试（两种格式、多产品批量）

- **FAQ文档**：新增 FAQ.md，20个高频问题
  - 基础概念（BEA是什么、W(T)是什么、亲极危极、六范式）
  - 使用方法（如何开始、如何校准t值、W(T)多少算好、品类权重）
  - 理论深入（与其他美学理论关系、核心命题、三级阈值、适用边界）
  - 实践应用（团队推广、适合/不适合场景、竞品分析方法）
  - 常见误区（6个典型误区及纠正方法）
  - 技术问题（安装、命令列表、JSON输出、贡献方式）

### 文档更新
- README.md 目录结构添加 FAQ.md 和 CHANGELOG.md

## v2.2.1（2026-09-14）

### 算法改进
- **suggest 调整策略重写**：从"优先调权重高的维度"改为"优先调当前 t 值偏离目标最远的维度"
  - 降低危极时：优先降 t 值最高的维度（如质感 6→1），而不是权重高但 t 值已低的维度（如形状 3→0）
  - 提升危极时：优先升 t 值最低的维度
  - 同时考虑权重：t 值相近时优先调权重高的维度
  - 连续调整同一维度，避免步骤细碎
  - 避免调到极端值 0 或 10（下限 1，上限 9）

- **攻击症诊断条件优化**：从 W(T)>0.60 且 t≥7 降低为 W(T)≥0.55 且 t≥6
  - 覆盖冷峻克制范式的高危极设计，避免漏报
  - 全 6 分 W(T)=0.6 现在会正确诊断为攻击症+重点通胀症

### 文档完善
- **病症诊断完整性**：代码中明确标注失序症、层级冲突症为"需人工判断"，报告中给出检查提示和处方
  - 失序症：检查是否缺少贯穿全局的统一主线
  - 层级冲突症：检查宏观/微观、跨通道的极性一致性
- 攻击症诊断条件在 01-core-theory.md 中同步更新

### 错误修复
- SKILL.md description 中版本号 v2.1 → v2.2
- SKILL.md 快速上手命令示例中的无效 `...` → 完整 6 维度示例
- 05-case-studies.md Model 3 案例 W(T) 计算错误 0.375 → 0.39
- install.sh 安装时未复制 LICENSE 文件 → 已添加

## v2.2.0（2026-09-13）

### 新增功能
- 量化引擎 bea_quant.py：analyze/report/score/suggest/compare/batch/template/test 八个命令
- 四维评分卡辅助（双极张力/结构秩序/阈值安全/语境适配）
- 病症自动诊断（甜腻症/攻击症/均分症/重点通胀症/张力不足症）
- 调整建议引擎（给定目标范式，输出维度变更方案）
- 紧凑格式/完整格式自动识别

### 文档体系
- 01-core-theory.md：核心理论、六范式、W(T)公式、四维评分、病症处方
- 02-workflow.md：45分钟评审流程、竞品分析、定调方法、6个常见误区
- 03-dimension-guide.md：四个品类每个维度的定义+真实产品锚点
- 04-checklist.md：设计自查清单（含5分钟快速版）
- 05-case-studies.md：真实案例库（iPhone Duo、S26 Ultra、Model 3、苹果品牌）

### 安装部署
- install.sh / install.ps1：macOS/Linux/Windows 一键安装
- update.sh / uninstall.sh：更新与卸载
- 三平台同步：GitHub / Gitee / ModelScope
