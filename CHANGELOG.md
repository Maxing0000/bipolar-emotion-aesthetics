# 更新日志

## v2.3.0（2026-09-14）

### 新增功能
- **灵敏度分析（sensitivity 命令）**：v3.0 务实版核心功能
  - 计算每个维度 t±1 对 W(T) 和四维评分的影响
  - 找出"改动哪个维度效果最明显"，避免盲目试错
  - 用有限差分法实现（不需要梯度下降），技术上完全可实现
  - 支持 `--target` 参数：计算朝目标范式方向最有效的调整
  - 支持 `--json` 输出，方便程序集成
  - 输出包含：核心结论（最值得改的3个维度）、朝目标方向最有效调整、详细灵敏度表、使用建议

### 算法改进
- 灵敏度分析的核心洞察：W(T) 变化量 = 权重/10，与当前 t 值无关（只要不在边界）
- 评分变化量通过实际重新计算四维评分获得，能发现"提升某维度会引入病症"的情况
- 边界值处理：t=9 时 t+1 变化为0，t=1 时 t-1 变化为0

### 测试
- 自测试从 42 项增加到 50 项，全部通过
- 新增测试：灵敏度分析返回所有维度、按灵敏度降序排列、包含必要字段、高权重维度灵敏度最高、带目标包含 toward_target、边界值处理

### 文档
- SKILL.md 版本号 v2.2 → v2.3，描述添加灵敏度分析和设计优化
- 命令速查添加 sensitivity 命令示例
- FAQ.md 添加灵敏度分析相关问题

## v2.2.2（2026-09-14）

### 新增功能
- **四维评分精细化**：从离散分值改为精细化评分
  - 双极张力：基础分15 + W(T)区间 + 维度极差 + 明确对比 - 病症扣分
  - 结构秩序：基础分15 + 焦点数量(1-2个最佳) + 无均分症 + 无重点通胀 - 甜腻症扣分
  - 阈值安全：基础分20 + W(T)安全区间 - 越界扣分 - 极端值扣分
  - 语境适配：参考分18 + 5项人工检查清单
  - 每个维度都有评分明细（基础分、各加扣分项及具体值）
  - 新增评级（成熟作品/良好/中等/存在明显问题）和短板提示

- **compare/batch JSON输出**：添加 `--json` 参数
  - compare JSON：两个产品完整分析 + 对比信息（wt_diff、paradigm_diff、dimension_diffs）
  - batch JSON：所有产品分析 + 统计信息（wt_min/wt_max/wt_avg）
  - 方便程序集成和自动化分析

- **自测试扩展**：从27项增加到42项
  - suggest 调整策略测试（降低危极优先降高t值、提升危极优先升低t值）
  - 攻击症新条件测试（W(T)=0.6全6分应诊断攻击症）
  - 边界值测试（W(T)=0.15/0.30/0.48/0.60/0.66 范式定位）
  - 四维评分测试（评分范围0-25、总分范围0-100、包含评分明细）
  - compare/batch 解析测试（两种格式、多产品批量）

- **FAQ文档**：新增 FAQ.md，20个高频问题
  - 基础概念（BEA是什么、W(T)是什么、亲极危极、六范式）
  - 使用方法（如何开始、如何校准t值、W(T)多少算好、品类权重）
  - 理论深入（与其他美学理论关系、核心命题、三级阈值、适用边界）
  - 实践应用（团队推广、适合/不适合场景、竞品分析方法）
  - 常见误区（6个典型误区及纠正方法）
  - 技术问题（安装、命令列表、JSON输出、贡献方式）

### 文档更新
- README.md 目录结构添加 FAQ.md 和 CHANGELOG.md

## v2.2.1（2026-09-14）

### 算法改进
- **suggest 调整策略重写**：从"优先调权重高的维度"改为"优先调当前 t 值偏离目标最远的维度"
  - 降低危极时：优先降 t 值最高的维度（如质感 6→1），而不是权重高但 t 值已低的维度（如形状 3→0）
  - 提升危极时：优先升 t 值最低的维度
  - 同时考虑权重：t 值相近时优先调权重高的维度
  - 连续调整同一维度，避免步骤细碎
  - 避免调到极端值 0 或 10（下限 1，上限 9）

- **攻击症诊断条件优化**：从 W(T)>0.60 且 t≥7 降低为 W(T)≥0.55 且 t≥6
  - 覆盖冷峻克制范式的高危极设计，避免漏报
  - 全 6 分 W(T)=0.6 现在会正确诊断为攻击症+重点通胀症

### 文档完善
- **病症诊断完整性**：代码中明确标注失序症、层级冲突症为"需人工判断"，报告中给出检查提示和处方
  - 失序症：检查是否缺少贯穿全局的统一主线
  - 层级冲突症：检查宏观/微观、跨通道的极性一致性
- 攻击症诊断条件在 01-core-theory.md 中同步更新

### 错误修复
- SKILL.md description 中版本号 v2.1 → v2.2
- SKILL.md 快速上手命令示例中的无效 `...` → 完整 6 维度示例
- 05-case-studies.md Model 3 案例 W(T) 计算错误 0.375 → 0.39
- install.sh 安装时未复制 LICENSE 文件 → 已添加

## v2.2.0（2026-09-13）

### 新增功能
- 量化引擎 bea_quant.py：analyze/report/score/suggest/compare/batch/template/test 八个命令
- 四维评分卡辅助（双极张力/结构秩序/阈值安全/语境适配）
- 病症自动诊断（甜腻症/攻击症/均分症/重点通胀症/张力不足症）
- 调整建议引擎（给定目标范式，输出维度变更方案）
- 紧凑格式/完整格式自动识别

### 文档体系
- 01-core-theory.md：核心理论、六范式、W(T)公式、四维评分、病症处方
- 02-workflow.md：45分钟评审流程、竞品分析、定调方法、6个常见误区
- 03-dimension-guide.md：四个品类每个维度的定义+真实产品锚点
- 04-checklist.md：设计自查清单（含5分钟快速版）
- 05-case-studies.md：真实案例库（iPhone Duo、S26 Ultra、Model 3、苹果品牌）

### 安装部署
- install.sh / install.ps1：macOS/Linux/Windows 一键安装
- update.sh / uninstall.sh：更新与卸载
- 三平台同步：GitHub / Gitee / ModelScope
