> Copyright (c) 2026 马星. Licensed under CC BY-NC-SA 4.0.

---
name: bipolar-emotion-aesthetics
description: "双极情绪美学（BEA）v2.5——把审美判断从'我觉得'变成可讨论、可比较、可追踪、可生成的协作工具。当用户需要分析、评价、诊断、优化、生成任何以感官形式呈现的对象（产品/手机/消费电子/汽车/建筑/室内/平面/品牌/Logo/字体/海报/UI/交互/服装/声音/影视）的美感、高级感、亲和力、攻击性、治愈感、冷峻感、崇高感、先锋感，或讨论圆润与尖锐、柔和与硬朗、繁与简、明与暗、甜腻、廉价感、张力、比例、配色情绪、形态气质、风格定位、设计评审时使用。支持：①自然语言分析——用户用大白话描述需求即可触发，如'这个设计怎么样''怎么改更高级''帮我设计一个震撼的汽车'；②图片分析——上传产品/设计/建筑/UI图片后识别元素极性、计算W(T)、定位范式、生成四维评分卡、给出病症诊断与元素级改进处方；③量化计算——通过bea_quant.py执行W(T)计算、范式定位、病症诊断、竞品对比、批量分析、灵敏度分析、主辅比计算、耐看性评估、跨模态一致性分析、风格周期律判断；④美感生成——给定目标范式/W(T)，自动生成最优维度配置（均衡/集中/分散三种策略）；⑤设计优化——灵敏度分析找出最值得改的维度，调整建议给出完整方案含验证；⑥设计评审——提供团队打分模板、评审流程、自查清单、真实案例参考。不用于：裁决内容美/道德美/思想美，非形式层的可用性、工程与安全评估，艺术创作评判。"
version: "2.5.0"
updated: "2026-09-15"
author: "马星"
license: "CC BY-NC-SA 4.0"
---

# 双极情绪美学（BEA）v2.5

## 多平台支持

BEA 技能可在多个 AI 平台使用，详见 `platforms/` 目录：

| 平台 | 适配方式 | 文档 |
|---|---|---|
| 豆包 Doubao | 原生 SKILL.md | 本文件 |
| Coze 扣子 | Bot System Prompt | `platforms/coze/` |
| ChatGPT / GPTs | Custom Instructions | `platforms/chatgpt/` |
| Claude | Project Instructions | `platforms/claude/` |
| 通义千问 | 智能体 System Prompt | `platforms/tongyi/` |
| Dify | 应用系统提示词 | `platforms/dify/` |

**通用 System Prompt**：`platforms/universal-system-prompt.md`，复制粘贴到任意 AI 平台即可使用。

**两种模式**：
- 纯文本模式（零依赖）：AI 按工作流程手动计算，适用于所有平台
- 脚本增强模式（精确计算）：调用 `scripts/bea_quant.py`，需平台支持代码执行

## 核心价值

**BEA 解决的根本问题不是"什么是美"，而是"美感无法被讨论"。**

设计评审中，"我觉得不好看"是私人判断，无法争论。BEA 把它拆成维度、标极性、算权重、定范式、开处方——让审美从**投票**变成**工程**。你可以不同意某个人打的分，但争论的是"这个圆角到底是亲极 2 还是 3"，而不是"你懂不懂审美"——前者有解，后者无解。

**核心命题**：美感 = 可控张力下的情绪奖赏。亲极（圆润/柔色/对称）安其心，危极（尖锐/强对比/坚硬）提其神，秩序统其乱，阈值守其界。

> 完整理论（六范式、W(T)公式、四维评分、病症处方）见 `references/01-core-theory.md`

## 使用者怎么用

### 方式1：自然语言对话（最常用）

用户不需要知道任何命令，直接用大白话提问，AI 自动识别并调用 BEA：

**分析类：**
- "帮我看看这个手机设计怎么样"
- "这个汽车造型给人什么感觉？"
- "这个 logo 为什么看起来有点廉价？"
- "这个 UI 界面是不是太乱了？"

**诊断类：**
- "这个设计有什么问题？"
- "为什么这个产品看起来不够高级？"
- "这个配色是不是太甜了？"
- "这个造型是不是太凶了？"

**优化类：**
- "怎么改能让它更有高级感？"
- "我想让这个设计更震撼一点"
- "帮我把这个调整到亲和精致风格"
- "改哪个地方效果最明显？"

**生成类：**
- "帮我设计一个崇高震撼风格的汽车外形"
- "给我一个亲和精致的手机配色方案"
- "生成一个治愈系的品牌视觉"

**对比类：**
- "这两个方案哪个更好？"
- "A方案和B方案的风格有什么区别？"

### 方式2：图片上传分析

用户上传产品/设计/建筑/UI 图片，AI 自动执行：
1. 视觉识别：形状、色彩、质感、构图、光影
2. 极性标注：每维度标方向（P亲/T危）和强度（0-10）
3. W(T)计算：调用 `bea_quant.py report`
4. 范式定位：对照六范式
5. 四维评分
6. 病症诊断（7种）
7. 改进处方：具体维度、目标强度、预期效果

### 方式3：精确量化计算

当用户需要精确数据时，AI 调用 `bea_quant.py` 执行量化计算（见下方命令速查）。

## AI 行为指引（重要）

当用户的问题涉及美学分析、设计评价、形式美感时，AI 应按以下流程执行：

### 触发判断

以下任一情况即应触发 BEA：
- 用户提到"好看/不好看/高级/廉价/凶/甜/乱/单调/震撼/治愈/冷峻/先锋"等审美评价词
- 用户要求分析产品/设计/建筑/UI/品牌/汽车/手机的外形或视觉
- 用户上传图片并要求评价/分析/改进
- 用户讨论"圆润vs尖锐""柔和vs硬朗""繁vs简""明vs暗"等形式对比
- 用户要求"设计一个XX风格的产品/品牌/界面"

### 执行流程

**1. 图片分析（用户上传图片时）：**
- 先用视觉能力识别图片中的形式元素（形状、色彩、质感、构图、光影）
- 按 BEA 维度打 t 值（0-10），不确定时说明"基于视觉判断的估计值"
- 调用 `bea_quant.py report` 计算 W(T) 和范式
- 输出：范式定位 + 主辅比 + 病症诊断 + 元素级改进处方

**2. 文本分析（用户用文字描述设计时）：**
- 从用户描述中提取各维度的极性信息
- 如果信息不足，主动询问 1-2 个关键问题（如"整体偏圆润还是偏锐利？"）
- 调用 `bea_quant.py report` 计算
- 输出分析报告

**3. 优化建议（用户问"怎么改"时）：**
- 先调用 `bea_quant.py report` 分析当前状态
- 再调用 `bea_quant.py suggest --target 目标范式` 生成调整方案
- 输出：当前问题 + 具体改法（哪个维度、改成多少）+ 预期效果

**4. 设计生成（用户要求"设计一个"时）：**
- 先确认目标范式（如不确定，给 2-3 个选项让用户选）
- 调用 `bea_quant.py generate --target 范式 --strategy balanced` 生成最优配置
- 输出：维度配置 + 范式说明 + 设计要点提示

**5. 方案对比（用户问"哪个更好"时）：**
- 分别获取两个方案的维度值
- 调用 `bea_quant.py compare` 对比
- 输出：W(T)差异 + 范式差异 + 维度差异 + 适用场景建议

### 输出格式

AI 输出应包含：
1. **一句话结论**：这个设计给人的整体感觉 + 范式定位
2. **关键数据**：W(T) 值 + 主辅比 + 四维评分
3. **问题诊断**：如果有病症，指出是哪种 + 证据
4. **改进建议**：具体到维度和数值（如果用户要求改进）
5. **用大白话解释**：避免术语堆砌，让非专业用户也能懂

### 注意事项

- 不要让用户记命令，AI 自动调用脚本
- 打分有主观性，说明"这是基于 BEA 框架的分析，供参考"
- 不要用 BEA 裁决内容美、道德美、艺术创作
- 如果用户只是闲聊审美话题，不需要调用脚本，用理论回答即可

## 快速上手（5分钟）

1. **看维度定义**：读 `references/03-dimension-guide.md`，知道每个维度看什么、有哪些真实产品锚点
2. **打一轮分**：对着分析对象按维度打 t 值（0-10），不确定先打中间值
3. **算 W(T)**：`python3 scripts/bea_quant.py report --category phone --t "形状=3,质感=6,色彩=4,构图=3,光影=5,细节=6"`
4. **看诊断**：报告给出范式和病症，对照 `references/04-checklist.md` 自查
5. **看案例**：不确定打得对不对？读 `references/05-case-studies.md` 校准

团队评审用 `templates/review-record.md` 记录。评审流程见 `references/02-workflow.md`。

## 量化引擎命令速查

`scripts/bea_quant.py`，仅用 Python 标准库，离线可用。

### 核心命令

```bash
# 1. 完整分析报告（人类可读）
python3 scripts/bea_quant.py report --category car --t "曲面=4,特征线=6,灯组=5,比例=3,材质=4"

# 2. 调整建议：给定目标范式/W(T)，输出维度变更方案（含调整后验证）
python3 scripts/bea_quant.py suggest --category car --t "曲面=4,特征线=6,灯组=5,比例=3,材质=4" --target 崇高震撼
# 支持两种策略：--strategy focused（集中，默认）/ distributed（分散）

# 3. ⭐ 美感生成：给定目标，自动生成最优维度配置（v2.5新增）
python3 scripts/bea_quant.py generate --category phone --target 0.30 --strategy balanced
# 三种策略：balanced（均衡）/ focused（集中）/ distributed（分散）

# 4. 灵敏度分析：找出改动哪个维度效果最明显
python3 scripts/bea_quant.py sensitivity --category car --t "曲面=4,特征线=6,灯组=5,比例=3,材质=4" --target 0.55 --step 1

# 5. 两个产品对比
python3 scripts/bea_quant.py compare --category car --a "方案A=4,6,5,3,4" --b "方案B=7,6,5,4,4"

# 6. 多组/跨模态分析（外形+内饰+声音等）
python3 scripts/bea_quant.py multigroup --category car --groups "外形=曲面=4,特征线=6,...;内饰=曲面=3,特征线=3,..."

# 7. 风格周期律判断
python3 scripts/bea_quant.py style-cycle --category car --wt 0.45
```

### 辅助命令

```bash
# 分析（JSON输出，适合程序处理）
python3 scripts/bea_quant.py analyze --category phone --t "形状=3,质感=6,..."

# 详细评分辅助（含每个维度的加分/扣分原因）
python3 scripts/bea_quant.py score --category phone --t "形状=3,质感=6,..."

# 批量分析多个产品（分号分隔）
python3 scripts/bea_quant.py batch --category phone --items "A=3,6,4,3,5,6;B=4,4,4,3,3,5"

# 生成分打模板（含维度顺序，用于紧凑格式）
python3 scripts/bea_quant.py template --category phone

# 自定义权重配置管理
python3 scripts/bea_quant.py list-profiles
python3 scripts/bea_quant.py save-profile --name "运动汽车" --weights '{"曲面":0.35,"特征线":0.30,...}'
python3 scripts/bea_quant.py delete-profile --name "运动汽车"

# 自测试（108项）
python3 scripts/bea_quant.py test
```

### 输入格式说明

- `analyze`/`report`/`score`/`suggest`/`sensitivity`/`generate` 用完整格式：`形状=3,质感=6,...`
- `compare`/`batch` 的每个产品支持两种格式（自动识别）：
  - 紧凑格式：`名称=3,6,4,3,5,6`（按 `template` 输出的维度顺序）
  - 完整格式：`名称=形状=3,质感=6,...`（带维度名，推荐，不易出错）
- `multigroup` 格式：`组名=维度=值,维度=值,...;组名=维度=值,...`

**支持品类：** `phone`（手机）/ `car`（汽车）/ `brand`（品牌平面）/ `ui`（界面）/ `building`（建筑）

## 7种审美病症

| 病症 | 优先级 | 识别特征 | 处方 |
|---|---|---|---|
| 本能越界 | 一级（一票否决） | 任何维度 t>=10，触及生理红线 | 删除或钝化，无风格借口 |
| 攻击症 | 二级（严重） | W(T)>=0.55 且有 t>=6，令人紧张 | 扩大亲极基底，降危极到范式区间 |
| 刺激疲劳 | 二级（严重） | W(T)>=0.65 且所有 t>=7，全程高能 | 安排亲极呼吸段，控制强调点 |
| 重点通胀症 | 三级（中等） | >=3个维度 t>=6，视觉噪音大 | 做减法，强调点压回1-2个 |
| 均分症 | 三级（中等） | 所有 t 在3-5，W(T) 0.35-0.45，极差<2 | 确立>=6:4主辅比 |
| 甜腻症 | 四级（轻微） | W(T)<0.25 且所有 t<=4，无锐度 | 高价值细节注入10%-20%危极 |
| 张力不足症 | 四级（轻微） | W(T)<0.35 且极差<2，无记忆点 | 1-2个维度提升到6+，制造对比 |

## 六范式与 W(T) 区间

| 范式 | W(T) 区间 | 核心体验 |
|---|---|---|
| 治愈松弛 | <0.15 | 放松、舒展、无攻击性 |
| 亲和精致 | 0.15-0.30 | 第一眼亲和，细看精密 |
| 均衡典雅 | 0.30-0.48 | 刚柔各半、克制端庄 |
| 崇高震撼 | 0.48-0.60 | 先屏息敬畏、后沉浸沉醉 |
| 冷峻克制 | 0.60-0.66 | 冷硬简、精准比例 |
| 先锋反叛 | 0.66-0.85 | 刺激、反叛、临界于不适 |
| 逼近越阈 | ≥0.85 | 非美区（真实不适） |

## 图片分析工作流

用户上传产品/设计图片时：
1. 视觉识别：形状、色彩、质感、构图、光影
2. 极性标注：每维度标方向（P亲/T危）和强度（0-10）
3. W(T)计算：调用 `bea_quant.py report`
4. 范式定位：对照六范式
5. 四维评分
6. 病症诊断
7. 改进处方：具体维度、目标强度、预期效果

## 重要边界

1. **W(T) 是协作刻度，不是物理常数**——误差带至少 ±0.1，不要纠结小数点
2. **不替代用户测试**——W(T) 高不等于用户喜欢
3. **不评判艺术创作**——先锋艺术刻意越阈，BEA 会误判
4. **只对形式美负责**——不裁决内容美、道德美、工程可行性
5. **均衡典雅不是"最好"**——根据产品定位选范式，不要默认往中间挤
6. **层级冲突症需人工判断**——宏观/微观一致性无法自动诊断

## 文档索引

| 文件 | 内容 | 何时读 |
|---|---|---|
| `references/01-core-theory.md` | 核心理论、六范式、W(T)公式、四维评分、病症处方、t值校准 | 需要深入理论时 |
| `references/02-workflow.md` | 45分钟评审流程、竞品分析模板、定调流程、6个常见误区 | 实际执行评审时 |
| `references/03-dimension-guide.md` | **五个品类每个维度的定义+真实产品锚点** | 打分前必读 |
| `references/04-checklist.md` | **设计自查清单**（含5分钟快速版） | 设计定稿前、评审前 |
| `references/05-case-studies.md` | **真实案例库**（iPhone、奔驰S级、尊界S800、光之教堂等） | 校准打分、参考写法 |
| `templates/review-record.md` | **评审记录标准模板** | 每次评审后填写存档 |

---

**BEA v2.5** — 审美协作基础设施
工具：bea_quant.py（分析/报告/评分/调整/生成/灵敏度/对比/跨模态/风格周期律）
测试：108项全部通过
